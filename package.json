{
  "name": "css-classes-lsp",
  "version": "0.1.0",
  "description": "Language Server for CSS class name navigation — jump from class usage to definition. Supports HTML, Vue, React, CSS/SCSS with nesting and BEM.",
  "author": "Daniel",
  "license": "MIT",
  "type": "module",
  "main": "dist/server.js",
  "bin": {
    "css-classes-lsp": "dist/server.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "watch": "tsc -p tsconfig.build.json --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src/",
    "clean": "rm -rf dist",
    "prepublishOnly": "npm run clean && npm run build"
  },
  "dependencies": {
    "fast-glob": "^3.3.2",
    "vscode-languageserver": "^9.0.1",
    "vscode-languageserver-textdocument": "^1.0.12",
    "vscode-uri": "^3.0.8"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ]
}
